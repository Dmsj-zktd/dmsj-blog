# 安全说明

## 报告安全问题

本仓库是公开个人技术博客源码。如果你发现安全漏洞（XSS、认证绕过、信息泄露、依赖漏洞等），
请先私信 GitHub 仓库所有者，不要创建公开 issue，避免漏洞被利用。

请提供：

- 影响范围与可复现步骤；
- 期望行为与实际行为；
- 环境信息（浏览器/系统/版本）；
- 如涉及 payload，请最小化并脱敏。

## 项目自身的安全基线

- Secrets 只存在于 gitignore 的 `.dev.vars` / Cloudflare Secrets，禁止提交；
- 后台仅允许 GitHub 白名单账号经 OAuth 登录；
- API 输入全部经过 Zod 校验，SQL 参数化，管理操作写入审计日志；
- 公共页面输出严格 CSP 与安全响应头；
- 依赖通过 Dependabot 每月跟踪，合并前必须通过 CI。
