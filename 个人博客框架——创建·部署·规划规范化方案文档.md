# 个人博客框架——创建·部署·规划规范化方案文档


## 一、总体架构设计

### 1.1 架构理念

本博客采用**边缘原生（Edge-Native）无服务器架构**，全栈运行于 Cloudflare 边缘网络之上，实现全球低延迟访问、零服务器运维成本与极致安全性。核心设计原则：

- **零服务器成本**：全部依赖 Cloudflare 免费套餐，无任何月度费用
- **内容与呈现分离**：Markdown 内容与前端展示解耦，便于内容持续扩展
- **混合渲染策略**：公开页面静态化（SSG），管理后台动态渲染（SSR）
- **安全优先**：多层防护体系，从边缘层到数据层纵深防御

### 1.2 技术栈选型

| 层级         | 技术选型                             | 选型理由                            |
| ------------ | ------------------------------------ | ----------------------------------- |
| **前端框架** | Astro 5                              | 混合 SSG/SSR、岛屿架构、极致性能    |
| **后端/API** | Hono v4（运行于 Cloudflare Workers） | 轻量、边缘原生、TypeScript 原生支持 |
| **样式方案** | Tailwind CSS 4                       | 原子化 CSS、零运行时、主题可配置    |
| **数据库**   | Cloudflare D1（SQLite）              | 边缘 SQL 数据库，免费额度充足       |
| **对象存储** | Cloudflare R2                        | 图片/附件存储，零出口费用           |
| **缓存层**   | Cloudflare KV                        | 热点数据缓存、访问计数              |
| **认证方案** | JWT + PBKDF2-SHA256                  | 无状态认证，边缘计算友好            |
| **评论系统** | Giscus（GitHub Discussions）         | 无需自建，数据永存                  |
| **部署平台** | Cloudflare Pages + Workers           | 统一边缘部署，全球 300+ 节点        |

### 1.3 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                 │
│    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│    │  桌面浏览器   │  │  移动浏览器   │  │  搜索引擎爬虫 │       │
│    └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Cloudflare 边缘层                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │           Cloudflare CDN（全球 300+ 节点）                │ │
│  │    • DDoS 防护  • SSL 终止  • 智能路由  • 静态缓存        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                              │                                  │
│              ┌───────────────┴───────────────┐                 │
│              ▼                               ▼                 │
│  ┌─────────────────────┐     ┌─────────────────────┐          │
│  │  Cloudflare Pages   │     │  Cloudflare Workers │          │
│  │  （前端 SSG 页面）   │     │  （Hono API 服务）   │          │
│  └─────────────────────┘     └─────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据存储层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  D1 数据库   │  │   R2 存储    │  │   KV 缓存    │        │
│  │  (SQLite)    │  │  (对象存储)   │  │  (键值存储)   │        │
│  │  • 文章/用户  │  │  • 图片文件   │  │  • 会话缓存   │        │
│  │  • 评论/分类  │  │  • 附件      │  │  • 热点数据   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      第三方服务层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  GitHub OAuth│  │  Giscus评论  │  │  Resend邮件  │        │
│  │  (第三方登录) │  │  (评论区)    │  │  (通知服务)   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

### 1.4 模块解耦机制

为实现“内容持续添加、分类灵活扩展”的目标，采用以下解耦策略：

**1. 内容层解耦**
- 所有文章以 Markdown 格式存储于 D1 数据库，通过 Frontmatter 元数据（标题、分类、标签、日期、摘要、封面等）驱动展示
- 分类采用**多级树形结构**（支持无限嵌套），标签采用**扁平体系**，两者独立管理、互不干扰
- 文章与分类/标签通过关联表（多对多）关联，新增分类无需修改文章结构

**2. 渲染层解耦**
- 利用 Astro 的 **Content Collections** 特性，定义统一的内容 schema
- 页面组件与数据源分离：组件仅负责展示，数据通过 API/数据库查询获取
- 主题系统独立于内容：配色、布局、字体等通过 CSS 变量 + 配置文件驱动

**3. 扩展点设计**
- 预留“模块注册表”：新增内容类型（如“算法心得”“嵌入式经验”“LLM/Agent”）仅需在配置中注册路由和模板
- 插件化评论/搜索/分析等可选功能，通过开关控制启用/禁用


## 二、界面交互设计规范

### 2.1 设计理念

融合“简奢、高级、创意”三大特质，借鉴以下优秀设计语言：

- **玻璃拟态（Glassmorphism）** ：利用 `backdrop-filter: blur()` 营造轻盈层次感
- **Neo-Brutalist 元素**：大胆边框、强烈阴影，赋予个性态度
- **微交互动画**：Framer Motion 驱动的页面过渡、悬停反馈、滚动驱动动画
- **极简排版**：衬线/无衬线字体混排，建立中西文混排最佳视觉基准

### 2.2 色彩与布局主题系统

提供**不少于 5 种**预设主题，用户可在管理后台一键切换：

| 主题名称            | 配色方案                     | 风格定位           |
| ------------------- | ---------------------------- | ------------------ |
| **Default（随机）** | 每次加载从所有主题中随机选取 | 惊喜感与多样性     |
| **极光**            | 深蓝紫渐变 + 青绿高光        | 深邃科技感         |
| **暖阳**            | 米白底 + 琥珀橙点缀          | 温暖阅读体验       |
| **暗夜**            | 纯黑底 + 霓虹蓝紫            | 沉浸式夜间模式     |
| **徕卡**            | 红黑白经典 + 机械快门转场    | 复古机械美学       |
| **极简白**          | 纯白底 + 灰阶层次            | 内容优先、极致克制 |

**主题实现机制**：
- 使用 CSS 自定义属性（`--color-primary`、`--color-bg`、`--font-family` 等）定义主题变量
- 主题配置存储于 D1 数据库 + KV 缓存，切换即时生效
- 支持**明暗模式**一键切换，且与色彩主题独立（即每个主题都有明暗两套变体）

### 2.3 交互体验规范

| 交互元素     | 规范要求                                                 |
| ------------ | -------------------------------------------------------- |
| **页面过渡** | 路由切换采用平滑淡入淡出 + 微位移（300ms），避免生硬跳转 |
| **文章列表** | 支持网格/列表双视图切换，卡片悬停有细腻的抬升 + 阴影变化 |
| **代码块**   | 语法高亮（Shiki）+ 一键复制 + 行号显示 + 流光滚动效果    |
| **滚动体验** | 平滑滚动（`scroll-behavior: smooth`）+ 阅读进度条        |
| **响应式**   | 移动端优先设计，导航栏自动折叠为汉堡菜单                 |
| **加载状态** | 骨架屏占位，避免白屏闪烁                                 |


## 三、部署方案（零成本·高安全）

### 3.1 部署架构

采用 **Cloudflare 全栈无服务器部署**方案，全部运行于免费套餐：

| 组件         | 部署目标           | 免费额度                         |
| ------------ | ------------------ | -------------------------------- |
| 前端静态页面 | Cloudflare Pages   | 无限带宽，500 次构建/月          |
| 后端 API     | Cloudflare Workers | 10 万次请求/天                   |
| 数据库       | Cloudflare D1      | 5 GB 存储，500 万行读取/月       |
| 对象存储     | Cloudflare R2      | 10 GB 存储，100 万次 A 类操作/月 |
| 缓存         | Cloudflare KV      | 1 GB 存储，100 万次读取/天       |

**总成本：$0/月**（在免费额度内完全覆盖个人博客流量）

### 3.2 部署工作流

```
[本地开发] → [Git Push] → [GitHub Actions 触发] → [构建] → [Cloudflare Pages 部署]
                                                              │
                                                              ▼
                                                      [D1 数据库迁移]
                                                              │
                                                              ▼
                                                      [KV 缓存预热]
                                                              │
                                                              ▼
                                                      [全球节点生效]
```

**详细步骤**：

1. **代码仓库**：GitHub 私有仓库（Free 套餐支持私有仓库 + Actions）
2. **CI/CD**：GitHub Actions 自动化构建与部署
   - 触发条件：`push` 到 `main` 分支
   - 构建命令：`pnpm build`
   - 部署命令：`wrangler pages deploy`
3. **数据库迁移**：每次部署自动执行 D1 迁移脚本
4. **缓存刷新**：部署完成后自动清除相关 KV 缓存

### 3.3 安全部署规范

| 安全措施          | 实现方式                                                     |
| ----------------- | ------------------------------------------------------------ |
| **HTTPS 强制**    | Cloudflare 自动提供免费 SSL 证书，强制 HSTS                  |
| **DDoS 防护**     | Cloudflare 内置 DDoS 防护，无需额外配置                      |
| **敏感信息隔离**  | 所有密钥（JWT Secret、API Token 等）通过 Cloudflare 环境变量注入，绝不提交至代码仓库 |
| **最小权限原则**  | Workers 仅授予必要的 D1/R2/KV 绑定权限                       |
| **CORS 严格配置** | 仅允许白名单域名跨域请求                                     |
| **速率限制**      | 通过 KV + Workers 实现请求限流（每 IP 每分钟 60 次）         |
| **WAF 规则**      | 配置 Cloudflare WAF 托管规则集，拦截常见攻击模式             |


## 四、管理后台安全设计

### 4.1 后台架构

管理后台采用**单页应用（SPA）** 形态，内嵌于同一 Astro 项目，通过 `/admin` 路由访问。前端使用 Vue 3 + Element Plus 构建。

### 4.2 认证与授权体系

**1. 多因素认证（MFA）**

| 认证层级           | 实现方式                                          |
| ------------------ | ------------------------------------------------- |
| **第一层**         | 用户名 + 强密码（PBKDF2-SHA256 哈希存储，加盐）   |
| **第二层**         | TOTP（基于时间的一次性密码），使用 `speakeasy` 库 |
| **第三层（可选）** | Cloudflare Turnstile 人机验证                     |

**2. 会话管理**

- JWT Token 存储于 `HttpOnly` + `Secure` + `SameSite=Strict` Cookie
- Token 有效期：短期（15 分钟）+ Refresh Token（7 天）双令牌机制
- 登录失败 5 次后锁定账号 15 分钟（通过 KV 记录失败计数）
- 支持“所有设备登出”功能

**3. 权限分级**

| 角色                | 权限范围                                                     |
| ------------------- | ------------------------------------------------------------ |
| **站主（Owner）**   | 全部权限：用户管理、系统配置、所有内容的增删改查             |
| **管理员（Admin）** | 内容管理：文章/分类/标签/评论的增删改查，不可修改系统配置和用户权限 |
| **作者（Author）**  | 仅可创建和编辑自己的文章（草稿及已发布），不可删除他人文章   |

### 4.3 审计日志

**所有管理操作均记录审计日志**，存储于 D1 数据库的 `audit_logs` 表：

| 字段          | 说明                                                        |
| ------------- | ----------------------------------------------------------- |
| `id`          | 日志唯一 ID                                                 |
| `user_id`     | 操作用户 ID                                                 |
| `action`      | 操作类型（CREATE/UPDATE/DELETE/LOGIN/LOGOUT/CONFIG_CHANGE） |
| `resource`    | 操作资源（article/user/comment/category/system）            |
| `resource_id` | 资源 ID                                                     |
| `ip_address`  | 操作来源 IP                                                 |
| `user_agent`  | 浏览器/设备信息                                             |
| `timestamp`   | 操作时间                                                    |
| `details`     | 变更前后 JSON Diff                                          |

**审计策略**：
- 日志保留 **180 天**，自动滚动清理
- 站主可导出审计日志为 CSV/JSON
- 异常操作（如非工作时间大量删除）触发邮件告警

### 4.4 后台安全加固

| 安全措施              | 说明                                           |
| --------------------- | ---------------------------------------------- |
| **后台路径隐藏**      | `/admin` 路径可自定义配置，避免默认路径暴露    |
| **IP 白名单（可选）** | 限制仅允许特定 IP 段访问后台                   |
| **操作二次确认**      | 删除、批量操作等危险动作需二次确认             |
| **CSRF 防护**         | 所有非 GET 请求携带 CSRF Token                 |
| **XSS 防护**          | 内容渲染采用 DOMPurify 净化，防止注入          |
| **SQL 注入防护**      | D1 + Drizzle ORM 参数化查询，天然免疫 SQL 注入 |


## 五、内容管理规范

### 5.1 内容分类体系

为支持“算法心得、嵌入式经验、LLM/Agent 经验”等多类型内容，建立以下分类规范：

```
📂 内容根目录
├── 📂 算法心得 (algorithms)
│   ├── 数据结构
│   ├── 排序算法
│   ├── 图论
│   └── 动态规划
├── 📂 嵌入式经验 (embedded)
│   ├── MCU 开发
│   ├── RTOS
│   ├── 通信协议
│   └── 硬件调试
├── 📂 LLM/Agent 经验 (llm-agent)
│   ├── 模型微调
│   ├── Agent 架构
│   ├── Prompt Engineering
│   └── 部署与推理
├── 📂 项目展示 (projects)
└── 📂 随想杂谈 (thoughts)
```

**扩展机制**：新增分类只需在后台管理界面创建，系统自动生成路由（`/category/:slug`）和聚合页面，无需修改代码。

### 5.2 文章 Frontmatter 规范

每篇文章必须包含以下元数据：

```yaml
---
title: "文章标题"
slug: "url-friendly-slug"          # 自动生成，可手动修改
description: "文章摘要（用于 SEO 和列表展示）"
category: "算法心得"                # 所属分类
tags: ["标签1", "标签2", "标签3"]  # 标签数组
date: 2026-09-05
updated: 2026-09-06                # 可选
cover: "/images/cover.jpg"         # 封面图（可选）
draft: false                       # 草稿状态
series: "系列名称"                 # 可选，用于系列文章
---
```

### 5.3 写作与发布流程

1. **草稿阶段**：文章状态为 `draft`，仅作者本人可见
2. **审核阶段**（可选）：提交审核，管理员可审阅
3. **发布阶段**：状态改为 `published`，公开可见
4. **更新机制**：已发布文章更新后自动记录版本历史


## 六、性能优化规范

### 6.1 性能目标

| 指标                    | 目标值  |
| ----------------------- | ------- |
| **首屏加载（LCP）**     | < 1.0s  |
| **首次输入延迟（FID）** | < 100ms |
| **累积布局偏移（CLS）** | < 0.1   |
| **Time to Interactive** | < 1.5s  |

### 6.2 优化策略

| 优化手段            | 实现方式                                |
| ------------------- | --------------------------------------- |
| **静态生成（SSG）** | 公开文章页面预渲染为纯静态 HTML         |
| **边缘缓存**        | KV 直写缓存，设置合理 TTL               |
| **图片优化**        | R2 + 自动 WebP 转换、懒加载、响应式图片 |
| **代码分割**        | Astro 岛屿架构，按需加载交互组件        |
| **字体优化**        | 系统字体栈优先 + Google Fonts 预连接    |
| **资源压缩**        | 构建时自动压缩 HTML/CSS/JS              |
| **PWA 支持**        | 离线缓存、添加到主屏幕                  |


## 七、安全与健壮性规范

### 7.1 纵深防御体系

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: 边缘防护（Cloudflare CDN）                        │
│  • DDoS 防护  • WAF  • SSL/TLS  • 速率限制                 │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: 应用防护（Workers/Pages）                         │
│  • 输入验证  • XSS 过滤  • CSRF Token  • CORS 严格配置     │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: 认证层                                           │
│  • MFA  • JWT + Refresh Token  • 会话管理  • 权限控制      │
├─────────────────────────────────────────────────────────────┤
│  Layer 4: 数据层（D1 + R2 + KV）                           │
│  • 参数化查询（防 SQL 注入） • 数据加密 • 备份与恢复       │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 数据备份策略

| 备份类型         | 频率      | 保留期 | 存储位置               |
| ---------------- | --------- | ------ | ---------------------- |
| **D1 完整备份**  | 每日      | 30 天  | Cloudflare D1 自动备份 |
| **R2 图片备份**  | 实时      | 永久   | R2 跨区域复制          |
| **Git 仓库**     | 每次 Push | 永久   | GitHub（私有仓库）     |
| **手动全量导出** | 按需      | 永久   | 本地 + 云端            |

### 7.3 监控与告警

| 监控项           | 告警阈值                | 通知方式        |
| ---------------- | ----------------------- | --------------- |
| **站点可用性**   | 连续 3 次 5xx 错误      | 邮件 + Telegram |
| **API 响应时间** | P95 > 2s                | 邮件            |
| **免费额度使用** | 超过 80%                | 邮件            |
| **异常登录**     | 异地/新设备登录         | 邮件            |
| **审计异常**     | 批量删除/非工作时间操作 | 邮件            |


## 八、验证策略

### 8.1 功能验证清单

| 验证项        | 验证方法                     | 验收标准                   |
| ------------- | ---------------------------- | -------------------------- |
| 文章 CRUD     | 后台创建/编辑/删除/发布文章  | 操作成功，前台实时更新     |
| 分类/标签管理 | 新增/修改/删除分类和标签     | 文章自动关联，列表正确聚合 |
| 主题切换      | 切换 5 种以上主题 + 明暗模式 | 所有页面样式正确切换       |
| 响应式        | 在 320px~1920px 宽度测试     | 无布局错乱，所有功能可用   |
| 评论系统      | 发布/回复/审核评论           | Giscus 正常工作            |
| 搜索引擎      | Google Search Console 验证   | 页面可被正确索引           |
| PWA           | Lighthouse PWA 审计          | 得分 > 90                  |

### 8.2 安全验证清单

| 验证项   | 验证方法                  | 验收标准                   |
| -------- | ------------------------- | -------------------------- |
| SQL 注入 | 手动注入测试 + 自动化扫描 | 所有注入尝试被拦截         |
| XSS      | 注入 `<script>` 标签      | 内容被净化，脚本不执行     |
| CSRF     | 跨站请求伪造测试          | 无 CSRF Token 的请求被拒绝 |
| 认证绕过 | 未登录访问后台接口        | 返回 401/403               |
| 权限越权 | 低权限用户访问高权限接口  | 返回 403                   |
| HTTPS    | SSL Labs 测试             | 评级 A+                    |

### 8.3 性能验证清单

| 验证项     | 验证方法                 | 验收标准 |
| ---------- | ------------------------ | -------- |
| LCP        | Lighthouse / WebPageTest | < 1.0s   |
| TTI        | Lighthouse               | < 1.5s   |
| 缓存命中率 | Cloudflare Analytics     | > 85%    |
| 全球延迟   | 多地域 Ping 测试         | < 200ms  |


## 九、额外补充约束与需求

### 9.1 开发约束

| 约束项              | 要求                                                    |
| ------------------- | ------------------------------------------------------- |
| **TypeScript 强制** | 所有代码必须使用 TypeScript，严格模式开启               |
| **代码规范**        | ESLint + Prettier 统一代码风格                          |
| **提交规范**        | Conventional Commits（`feat:`/`fix:`/`docs:`/`chore:`） |
| **测试覆盖**        | 核心逻辑（认证、权限、数据库操作）单元测试覆盖率 > 80%  |
| **文档要求**        | 每个模块需有 README 说明（架构、接口、使用方式）        |

### 9.2 可维护性要求

- **配置集中化**：所有可配置项（站点名称、社交链接、导航菜单等）集中于 `site.config.ts`
- **环境隔离**：开发/测试/生产环境通过 `.env` 文件隔离
- **日志规范**：统一日志格式（JSON），包含 `timestamp`、`level`、`module`、`message`
- **依赖管理**：定期（每月）更新依赖，关注安全漏洞（Dependabot 自动 PR）

### 9.3 SEO 与可发现性

| SEO 措施       | 实现方式                                    |
| -------------- | ------------------------------------------- |
| **结构化数据** | JSON-LD 注入（Article、Breadcrumb、Person） |
| **Meta 标签**  | 自动生成 title、description、OG 图          |
| **Sitemap**    | 自动生成 `sitemap.xml`                      |
| **RSS Feed**   | 自动生成订阅源                              |
| **URL 规范**   | 语义化 Slug，永久链接（Permalink）          |

### 9.4 国际化支持

- 内置中英文双语切换
- 文章内容支持多语言版本（通过 `lang` 字段区分）
- 日期/数字格式根据语言环境自动适配


## 十、项目实施路线图

| 阶段                  | 里程碑                                           |
| --------------------- | ------------------------------------------------ |
| **Phase 1：基础搭建** | 项目初始化、Astro + Tailwind 配置、D1 数据库建表 |
| **Phase 2：核心功能** | 文章 CRUD、分类/标签、前台列表+详情页            |
| **Phase 3：管理后台** | 后台 UI、认证系统、权限控制、审计日志            |
| **Phase 4：主题系统** | 5+ 种主题、明暗模式、配置化管理                  |
| **Phase 5：部署上线** | Cloudflare 部署、域名绑定、CI/CD 配置            |
| **Phase 6：安全加固** | 安全测试、WAF 配置、备份策略落地                 |
| **Phase 7：内容迁移** | 已有内容迁移、分类体系建立                       |


